# Local configuration files
*.local
.mcp.json

# Backup and temporary files
*.bak
*.swp

# IDE / Editor directories
.vscode/
.idea/

# SSH private key — public key (cloud-systems.pub) is tracked, private key must not be
cloud-systems

# OpenTofu/Terraform working directory
.terraform/
*.tfstate
*.tfstate.backup
terraform.tfvars

.claude/
